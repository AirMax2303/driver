import 'package:flutter/cupertino.dart';

class DriverChats extends StatelessWidget {
  const DriverChats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
